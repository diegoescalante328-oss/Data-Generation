# CSV Generator Wrapper

This module keeps the recommended CLI namespace under `generators/` while reusing the existing rigorous implementation in `csv_generator/`.

## Run

```bash
python -m generators.csv_generator.generator.cli generate --schema csv_generator/schemas/retail_basic.yaml --out csv_generator/output
```
